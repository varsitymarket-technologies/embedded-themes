<!-- ════════════════════════════════ SHOP PAGE ════════════════════════════════ -->

<section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-8 pb-12">
    <div class="flex flex-col sm:flex-row sm:items-end sm:justify-between gap-6 mb-12">
        <div>
            <h1 class="font-display text-4xl sm:text-5xl font-bold text-black">Shop</h1>
            <p class="text-gray-600 text-sm mt-2">All products</p>
        </div>
        <div class="flex items-center gap-4">
            <label class="text-xs uppercase text-gray-600 font-semibold">Sort by</label>
            <select class="bg-white border border-gray-300 text-sm px-3 py-2 hover:border-black transition-colors outline-none">
                <option>Latest</option>
                <option>Price: Low to High</option>
                <option>Price: High to Low</option>
            </select>
        </div>
    </div>

    <!-- Filter tabs -->
    <div class="flex gap-2 mb-12 overflow-x-auto pb-2 border-b border-gray-200">
        <a href="/shop" class="text-sm font-medium text-black whitespace-nowrap pb-3 border-b-2 border-black">All</a>
        <a href="/shop?cat=essentials" class="text-sm font-medium text-gray-600 hover:text-black whitespace-nowrap pb-3 border-b-2 border-transparent hover:border-gray-300 transition-colors">Essentials</a>
        <a href="/shop?cat=new" class="text-sm font-medium text-gray-600 hover:text-black whitespace-nowrap pb-3 border-b-2 border-transparent hover:border-gray-300 transition-colors">New</a>
        <a href="/shop?cat=sale" class="text-sm font-medium text-gray-600 hover:text-black whitespace-nowrap pb-3 border-b-2 border-transparent hover:border-gray-300 transition-colors">Sale</a>
    </div>

    <!-- Product grid -->
    <div class="grid grid-cols-2 md:grid-cols-4 gap-4 sm:gap-6">
        <!-- #!/engine/node/
        # echo "Executing Engine Shop User Interface"; 
        -->

        <!-- #!/engine/node/
        app()->create_shop_ui(api()->async_products());
        -->
    </div>
</section>
