<!-- ══════════════════════════════ CONTACT PAGE ═════════════════════════════ -->

<?php
$sent = false;
if ($_SERVER['REQUEST_METHOD'] === 'POST' && !empty($_POST['message'])) {
    $sent = true;
}
?>

<section class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 pt-8 pb-24">
    <h1 class="font-display text-4xl font-bold text-black mb-2">Get in Touch</h1>
    <p class="text-gray-600 mb-12">We'd love to hear from you. Send us a message.</p>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-12">

        <!-- Info -->
        <div class="space-y-8">
            <div>
                <p class="text-xs uppercase font-semibold text-gray-700 mb-2">Email</p>
                <a href="mailto:hello@essencestore.co.za" class="text-gray-600 hover:text-black transition-colors">
                    hello@essencestore.co.za
                </a>
            </div>
            <div>
                <p class="text-xs uppercase font-semibold text-gray-700 mb-2">Phone</p>
                <p class="text-gray-600">+27 11 000 0000</p>
            </div>
            <div>
                <p class="text-xs uppercase font-semibold text-gray-700 mb-2">Hours</p>
                <p class="text-gray-600">Monday – Friday<br>9am – 5pm SAST</p>
            </div>
        </div>

        <!-- Form -->
        <div>
            <?php if ($sent): ?>
                <div class="text-center py-12">
                    <i data-lucide="check-circle" class="w-12 h-12 text-gray-900 mx-auto mb-4"></i>
                    <p class="font-semibold text-black mb-1">Message received</p>
                    <p class="text-sm text-gray-600">We'll get back to you within one business day.</p>
                </div>
            <?php else: ?>
                <form method="POST" action="/contact" class="space-y-4">
                    <input type="text" name="name" placeholder="Your name" required
                        class="w-full border border-gray-300 bg-white px-4 py-3 text-sm outline-none focus:border-black transition-colors">
                    <input type="email" name="email" placeholder="Your email" required
                        class="w-full border border-gray-300 bg-white px-4 py-3 text-sm outline-none focus:border-black transition-colors">
                    <select name="subject"
                        class="w-full border border-gray-300 bg-white px-4 py-3 text-sm outline-none focus:border-black transition-colors text-gray-700">
                        <option value="">Select topic</option>
                        <option>Order enquiry</option>
                        <option>Returns</option>
                        <option>Product question</option>
                        <option>General</option>
                    </select>
                    <textarea name="message" placeholder="Your message" rows="6" required
                        class="w-full border border-gray-300 bg-white px-4 py-3 text-sm outline-none focus:border-black transition-colors resize-none"></textarea>
                    <button type="submit" class="w-full bg-black text-white py-3 text-sm font-semibold hover:bg-gray-800 transition-colors">
                        Send
                    </button>
                </form>
            <?php endif; ?>
        </div>

    </div>
</section>
