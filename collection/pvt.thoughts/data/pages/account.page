<?php
if (session_status() === PHP_SESSION_NONE) {
    session_start();
}

$store = $_SESSION['store'] ?? [];
$customer = $store['customer'] ?? [];
$orders = $store['orders'] ?? [];
$lastOrder = $store['last_order'] ?? null;
$storedEmail = $_COOKIE['pvt_customer_email'] ?? ($customer['email'] ?? '');
?>
<main class="account-section">
    <div class="container" style="padding-top: 2rem; padding-bottom: 4rem; max-width: 1160px;">
        <div class="card-machined policy-card" style="padding: 2rem; margin-bottom: 1.5rem;">
            <div style="display: flex; justify-content: space-between; gap: 1rem; flex-wrap: wrap; align-items: end;">
                <div>
                    <div style="font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.08em; color: var(--text-subtle);">Account</div>
                    <h1 style="font-size: clamp(2rem, 4vw, 3rem); margin: 0.25rem 0;">Dashboard</h1>
                    <p style="color: var(--text-subtle);">Manage profile details, review orders, and continue shopping from one place.</p>
                </div>
                <div style="display: flex; gap: 0.75rem; flex-wrap: wrap;">
                    <a class="btn btn-outline" href="?page=orders">View Orders</a>
                    <a class="btn btn-acid" href="?page=checkout">Checkout</a>
                </div>
            </div>
        </div>

        <div style="display: grid; grid-template-columns: 1.1fr 0.9fr; gap: 1.5rem; align-items: start;">
            <section class="card-machined policy-card" style="padding: 2rem;">
                <h2 style="margin-bottom: 1rem;">Profile</h2>
                <div style="display: grid; gap: 0.85rem; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));">
                    <div style="padding: 1rem; background: var(--bg-card-subtle); border: 1px solid var(--border-medium);">
                        <div style="font-size: 0.75rem; color: var(--text-subtle); text-transform: uppercase;">Name</div>
                        <div style="font-weight: 700;"><?= htmlspecialchars((string)($customer['name'] ?? 'Guest Shopper')) ?></div>
                    </div>
                    <div style="padding: 1rem; background: var(--bg-card-subtle); border: 1px solid var(--border-medium);">
                        <div style="font-size: 0.75rem; color: var(--text-subtle); text-transform: uppercase;">Email</div>
                        <div style="font-weight: 700;"><?= htmlspecialchars((string)$storedEmail) ?></div>
                    </div>
                    <div style="padding: 1rem; background: var(--bg-card-subtle); border: 1px solid var(--border-medium);">
                        <div style="font-size: 0.75rem; color: var(--text-subtle); text-transform: uppercase;">Saved Address</div>
                        <div style="font-weight: 700;"><?= htmlspecialchars(trim(($customer['city'] ?? '') . ' ' . ($customer['state'] ?? ''))) ?></div>
                    </div>
                    <div style="padding: 1rem; background: var(--bg-card-subtle); border: 1px solid var(--border-medium);">
                        <div style="font-size: 0.75rem; color: var(--text-subtle); text-transform: uppercase;">Last Order</div>
                        <div style="font-weight: 700;"><?= htmlspecialchars((string)($lastOrder['order_number'] ?? 'None yet')) ?></div>
                    </div>
                </div>
            </section>

            <aside style="display: grid; gap: 1rem;">
                <div class="card-machined policy-card" style="padding: 1.5rem;">
                    <h3 style="margin-bottom: 0.75rem;">Quick Actions</h3>
                    <div style="display: grid; gap: 0.75rem;">
                        <a class="btn btn-outline" href="?page=shop">Continue Shopping</a>
                        <a class="btn btn-outline" href="?page=track-order">Track an Order</a>
                        <a class="btn btn-outline" href="?page=wishlist">Wishlist</a>
                    </div>
                </div>

                <div class="card-machined policy-card" style="padding: 1.5rem;">
                    <h3 style="margin-bottom: 0.75rem;">Store Stats</h3>
                    <div style="display: grid; gap: 0.75rem;">
                        <div style="display: flex; justify-content: space-between;"><span style="color: var(--text-subtle);">Orders</span><strong><?= count($orders) ?></strong></div>
                        <div style="display: flex; justify-content: space-between;"><span style="color: var(--text-subtle);">Checkout Status</span><strong><?= htmlspecialchars((string)($store['checkout_status'] ?? 'idle')) ?></strong></div>
                        <div style="display: flex; justify-content: space-between;"><span style="color: var(--text-subtle);">Session Cart Items</span><strong><?= isset($store['cart']) ? count($store['cart']) : 0 ?></strong></div>
                    </div>
                </div>
            </aside>
        </div>
    </div>
</main>
