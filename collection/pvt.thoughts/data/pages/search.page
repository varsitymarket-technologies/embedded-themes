<?php
$query = trim((string)($_GET['q'] ?? ''));
$all = api()->async_products();
$res = $query === '' ? $all : array_filter($all, function ($item) use ($query) {
    $haystack = strtolower(implode(' ', [
        $item['name'] ?? '',
        $item['description'] ?? '',
        $item['category'] ?? ($item['category_name'] ?? '')
    ]));
    return str_contains($haystack, strtolower($query));
});
?>
<main class="search-section">
    <div class="container" style="padding-top: 2rem; padding-bottom: 4rem; max-width: 900px;">
        <div class="policy-card card-machined" style="padding: 2rem; margin-bottom: 1.5rem;">
            <h1 style="margin-bottom: 1rem;">Search Store</h1>
            <input type="search" value="<?= htmlspecialchars($query) ?>" placeholder="Search products, collections, or support topics" style="width: 100%; padding: 1rem 1.1rem; background: var(--bg-card-subtle); border: 1px solid var(--border-medium); color: var(--text-main);">
        </div>
        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1rem;">
            <!-- #!/engine/node/
            app()->create_shop_ui(array_values($res));
            -->
        </div>
    </div>
</main>
