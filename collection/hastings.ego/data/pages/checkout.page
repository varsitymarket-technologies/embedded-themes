<!-- ══════════════════════════════ CHECKOUT PAGE ═════════════════════════════ -->

<?php
$cart  = $_SESSION['cart'] ?? [];
$total = 0.0;
foreach ($cart as $item) {
    $raw = preg_replace('/[^0-9.]/', '', $item['price']);
    $total += (float)$raw * (int)$item['qty'];
}
?>

<section class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 pt-8 pb-24">
    <h1 class="font-display text-4xl font-bold text-black mb-12">Checkout</h1>

    <?php if (empty($cart)): ?>
        <div class="text-center py-24">
            <p class="font-display text-2xl text-black mb-4">Your cart is empty</p>
            <a href="<!-- #!/engine/node/ page('shop') -->" class="inline-block bg-black text-white px-8 py-3 text-sm font-semibold hover:bg-gray-800 transition-colors">
                Go Shopping
            </a>
        </div>
    <?php else: ?>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-12">

        <!-- Form -->
        <div class="md:col-span-2">
            <form method="POST" action="/orders" class="space-y-8">
                <input type="hidden" name="action" value="place_order">

                <fieldset>
                    <legend class="text-sm font-semibold uppercase text-black mb-6">Contact</legend>
                    <div class="space-y-4">
                        <div class="grid grid-cols-2 gap-4">
                            <input type="text" name="first_name" placeholder="First name" required
                                class="border border-gray-300 bg-white px-4 py-3 text-sm outline-none focus:border-black transition-colors">
                            <input type="text" name="last_name" placeholder="Last name" required
                                class="border border-gray-300 bg-white px-4 py-3 text-sm outline-none focus:border-black transition-colors">
                        </div>
                        <input type="email" name="email" placeholder="Email" required
                            class="w-full border border-gray-300 bg-white px-4 py-3 text-sm outline-none focus:border-black transition-colors">
                        <input type="tel" name="phone" placeholder="Phone (optional)"
                            class="w-full border border-gray-300 bg-white px-4 py-3 text-sm outline-none focus:border-black transition-colors">
                    </div>
                </fieldset>

                <fieldset class="border-t border-gray-200 pt-8">
                    <legend class="text-sm font-semibold uppercase text-black mb-6">Shipping</legend>
                    <div class="space-y-4">
                        <input type="text" name="address" placeholder="Street address" required
                            class="w-full border border-gray-300 bg-white px-4 py-3 text-sm outline-none focus:border-black transition-colors">
                        <input type="text" name="apartment" placeholder="Apartment, suite, etc. (optional)"
                            class="w-full border border-gray-300 bg-white px-4 py-3 text-sm outline-none focus:border-black transition-colors">
                        <div class="grid grid-cols-2 gap-4">
                            <input type="text" name="city" placeholder="City" required
                                class="border border-gray-300 bg-white px-4 py-3 text-sm outline-none focus:border-black transition-colors">
                            <input type="text" name="postal" placeholder="Postal code" required
                                class="border border-gray-300 bg-white px-4 py-3 text-sm outline-none focus:border-black transition-colors">
                        </div>
                        <select name="province" required
                            class="w-full border border-gray-300 bg-white px-4 py-3 text-sm outline-none focus:border-black transition-colors text-gray-700">
                            <option value="">Select province</option>
                            <option>Gauteng</option>
                            <option>Western Cape</option>
                            <option>KwaZulu-Natal</option>
                            <option>Eastern Cape</option>
                            <option>Limpopo</option>
                        </select>
                    </div>
                </fieldset>

                <fieldset class="border-t border-gray-200 pt-8">
                    <legend class="text-sm font-semibold uppercase text-black mb-6">Payment</legend>
                    <div class="space-y-3">
                        <label class="flex items-center gap-3 border border-black p-4 cursor-pointer">
                            <input type="radio" name="payment" value="eft" checked class="w-4 h-4 accent-black">
                            <span class="text-sm font-medium">Bank Transfer / EFT</span>
                        </label>
                        <label class="flex items-center gap-3 border border-gray-300 p-4 cursor-pointer hover:border-black transition-colors">
                            <input type="radio" name="payment" value="card" class="w-4 h-4 accent-black">
                            <span class="text-sm font-medium">Credit / Debit Card</span>
                        </label>
                    </div>
                </fieldset>

                <button type="submit"
                    class="w-full bg-black text-white py-4 text-sm font-semibold hover:bg-gray-800 transition-colors">
                    Complete Purchase
                </button>
            </form>
        </div>

        <!-- Order summary -->
        <div class="h-fit">
            <div class="bg-gray-50 p-8 sticky top-24">
                <h3 class="font-display text-lg font-bold text-black mb-6">Order Summary</h3>
                <div class="space-y-4 mb-6 pb-6 border-b border-gray-200">
                    <?php foreach ($cart as $item): ?>
                    <div class="flex justify-between text-sm">
                        <span class="text-gray-600">
                            <?= htmlspecialchars($item['name']) ?> ×<?= $item['qty'] ?>
                        </span>
                        <span class="text-black font-medium"><?= htmlspecialchars($item['price']) ?></span>
                    </div>
                    <?php endforeach; ?>
                </div>
                <div class="space-y-2 text-sm mb-4">
                    <div class="flex justify-between text-gray-600">
                        <span>Subtotal</span>
                        <span>R<?= number_format($total, 2) ?></span>
                    </div>
                    <div class="flex justify-between text-gray-600">
                        <span>Shipping</span>
                        <span>TBC</span>
                    </div>
                </div>
                <div class="flex justify-between font-semibold text-base border-t border-gray-200 pt-4">
                    <span>Total</span>
                    <span>R<?= number_format($total, 2) ?></span>
                </div>
            </div>
        </div>

    </div>
    <?php endif; ?>
</section>
