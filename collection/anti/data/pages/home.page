<?php
$featured_products = array_slice(api()->async_products(), 0, 4);
$featured_categories = array_slice(api()->async_categories(), 0, 4);
?>

<div class="pt-32 pb-20">
    <div class="relative mb-20 px-6 max-w-7xl mx-auto">
        <div class="absolute inset-0 bg-black opacity-60 rounded-3xl"></div>
        <div class="relative z-10 py-20 md:py-32 text-center md:text-left">
            <h1 class="text-6xl md:text-8xl font-black tracking-tighter text-white leading-[1.1]">ANTI-<br>CLOTHING</h1>
            <p class="text-zinc-300 text-lg max-w-xl mt-6 mx-auto md:mx-0">
                ANTI strips away the noise and keeps the focus on dark, direct, utilitarian clothing.
            </p>
            <div class="flex gap-4 justify-center md:justify-start mt-8">
                <button onclick="navigate(&#39;collection&#39;)"
                    class="bg-white text-black px-8 py-3 rounded-full font-bold text-sm hover:bg-gray-200 transition">VISIT
                    SHOP</button>
                <button onclick="navigate(&#39;categories&#39;)"
                    class="border border-white/30 px-8 py-3 rounded-full text-sm hover:border-white transition">EXPLORE</button>
            </div>
        </div>
    </div>

    <div class="max-w-7xl mx-auto px-6 mb-20">
        <h2 class="text-3xl font-bold mb-8 border-l-4 border-white pl-4">FEATURED DROPS</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6" id="featured-grid">
            <!-- #!/engine/node/
            app()->create_shop_ui($featured_products);
            -->
        </div>
    </div>

    <div class="max-w-6xl mx-auto px-6 mb-20">
        <div class="border border-white/10 rounded-3xl p-8 md:p-12 bg-black/40">
            <div class="flex flex-col md:flex-row gap-12 items-center">
                <div class="md:w-1/3">
                    <img src="./index_files/logo.png" alt="Anti"
                        class="rounded-2xl w-full h-auto grayscale border border-white/20">
                </div>
                <div class="md:w-2/3">
                    <div class="text-sm uppercase tracking-wider text-zinc-500 mb-2">FROM THE FOUNDER</div>
                    <h2 class="text-3xl md:text-4xl font-bold mb-6">Demetrius</h2>
                    <p class="text-zinc-300 leading-relaxed mb-4">
                        ANTI is built around utilitarian clothing, raw silhouettes, and a direct approach to design.
                        The aim is simple: make pieces that feel permanent.
                    </p>
                    <p class="text-zinc-400 text-sm italic">— Founder &amp; Creative Director</p>
                </div>
            </div>
        </div>
    </div>

    <div class="max-w-7xl mx-auto px-6 pb-20">
        <h2 class="text-3xl font-bold mb-8 border-l-4 border-white pl-4">FEATURED CATEGORIES</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
            <!-- #!/engine/node/
            app()->create_category_ui($featured_categories);
            -->
        </div>
    </div>
</div>
