
    <!-- MAIN CONTENT - COMMS PROTOCOL (CONTACT US) -->
    <main class="flex-grow pt-32 pb-20 max-w-7xl mx-auto px-6 md:px-10 w-full reveal">
        
        <!-- HEADER & TELEMETRY -->
        <div class="flex flex-col md:flex-row justify-between items-start md:items-end mb-16 pb-8 border-b specimen-border gap-6">
            <div>
                <p class="font-bold uppercase opacity-40 mb-4">Feel Free To Contact Us</p>
                <h1 class="font-display font-black text-4xl md:text-6xl uppercase tracking-tighter">
                    Contact Us
                </h1>
            </div>
        </div>

        <!-- CONTACT GRID LAYOUT -->
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 lg:gap-16 items-start">
            
            <!-- TRANSMISSION FORM (Left 7 Columns) -->
            <div class="lg:col-span-7 border specimen-border p-8 md:p-12 bg-white/40">
                <div class="mb-10 pb-6 border-b specimen-border">
                    <h2 class="font-display font-black text-2xl uppercase tracking-tight">Here To Support </h2>
                    <p class="text-[10px] uppercase opacity-40 mt-2">Please fill in the information and we will get back to you.</p>
                </div>

                <form id="contact-form" class="space-y-8">
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                        <div class="space-y-3">
                            <label class="block text-[10px] font-bold uppercase tracking-[0.3em] opacity-60">Name</label>
                            <input type="text" id="contact-name" placeholder="FULL NAME" required class="w-full bg-transparent border specimen-border p-4 text-xs font-mono uppercase focus:border-archive-ink outline-none placeholder:opacity-30">
                        </div>
                        <div class="space-y-3">
                            <label class="block text-[10px] font-bold uppercase tracking-[0.3em] opacity-60">Email</label>
                            <input type="email" id="contact-email" placeholder="EMAIL ADDRESS" required class="w-full bg-transparent border specimen-border p-4 text-xs font-mono uppercase focus:border-archive-ink outline-none placeholder:opacity-30">
                        </div>
                    </div>

                    <div class="space-y-3">
                            <label class="block text-[10px] font-bold uppercase tracking-[0.3em] opacity-60">Phone</label>
                            <input type="phone" id="contact-phone" placeholder="PHONE NUMBER" required class="w-full bg-transparent border specimen-border p-4 text-xs font-mono uppercase focus:border-archive-ink outline-none placeholder:opacity-30">
                        </div>

                    <div class="space-y-3">
                        <label class="block text-[10px] font-bold uppercase tracking-[0.3em] opacity-60">Message</label>
                        <textarea id="contact-message" rows="5" placeholder="ENTER MESSAGE DETAILS..." required class="w-full bg-transparent border specimen-border p-4 text-xs font-mono uppercase focus:border-archive-ink outline-none placeholder:opacity-30 resize-none"></textarea>
                    </div>

                    <button type="submit" id="submit-btn" class="w-full bg-archive-ink text-archive-paper py-5 text-xs font-bold uppercase tracking-[0.4em] hover:opacity-90 transition-opacity">
                        Submit Form
                    </button>
                </form>
            </div>

            <!-- STATION TELEMETRY & DIRECT COORDS (Right 5 Columns) -->
            <div class="lg:col-span-5 space-y-8 lg:sticky lg:top-32">
                
                <div class="border specimen-border p-8 bg-archive-ink text-archive-paper space-y-4">
                    <h3 class="font-display font-black text-xl uppercase tracking-tight pb-4">Reach Us Directly</h3>
                    
                    <div class="space-y-4 text-xs uppercase">
                        <div>
                            <p class="text-[9px] opacity-40 uppercase mb-1">Support Email</p>
                            <p class="font-bold">support@voide.com</p>
                        </div>
                        <div>
                            <p class="text-[9px] opacity-40 uppercase mb-1">Phone NUmbers</p>
                            <p class="font-bold">+27 (0) 11 000 4040</p>
                        </div>
                        <div>
                            <p class="text-[9px] opacity-40 uppercase mb-1">Location</p>
                            <p class="font-bold">45 Bougainvillea, Montana Tuine, Gauteng, ZA</p>
                        </div>
                    </div>
                </div>

            </div>

        </div>

    </main>


    <!-- CONTACT LOGIC -->
    <script>
        class ContactController {
            constructor() {
                this.initForm();
                this.initMobileMenu();
                this.updateCartCount();
            }

            initForm() {
                const form = document.getElementById('contact-form');
                const btn = document.getElementById('submit-btn');

                form.addEventListener('submit', (e) => {
                    e.preventDefault();
                    
                    btn.textContent = 'TRANSMITTING PACKET...';
                    btn.classList.add('bg-emerald-900');

                    setTimeout(() => {
                        btn.textContent = 'TRANSMISSION RECEIVED [+]';
                        alert('Transmission logged successfully. Archival command will respond via secure return coordinates shortly.');
                        form.reset();
                        
                        setTimeout(() => {
                            btn.textContent = 'TRANSMIT_MESSAGE';
                            btn.classList.remove('bg-emerald-900');
                        }, 2000);
                    }, 1200);
                });
            }

            initMobileMenu() {
                const btn = document.getElementById('menu-toggle');
                const menu = document.getElementById('mobile-menu');
                let isOpen = false;

                btn.addEventListener('click', () => {
                    isOpen = !isOpen;
                    if (isOpen) {
                        menu.classList.remove('menu-closed');
                        menu.classList.add('menu-open');
                        btn.textContent = '[ CLOSE ]';
                    } else {
                        menu.classList.remove('menu-open');
                        menu.classList.add('menu-closed');
                        btn.textContent = '[ MENU ]';
                    }
                });
            }

            updateCartCount() {
                const cart = JSON.parse(localStorage.getItem('specimen_cart')) || [];
                const count = cart.reduce((acc, item) => acc + item.qty, 0);
                document.querySelectorAll('.js-cart-count').forEach(el => el.textContent = count);
            }
        }

        document.addEventListener('DOMContentLoaded', () => {
            window.ContactApp = new ContactController();
        });
    </script>
