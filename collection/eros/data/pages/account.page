<?php
if (session_status() === PHP_SESSION_NONE) {
    session_start();
}
$store = $_SESSION['eros'] ?? [];
$customer = $store['customer'] ?? [];
$orders = $store['orders'] ?? [];
$lastOrder = $store['last_order'] ?? null;
$email = $_COOKIE['eros_customer_email'] ?? ($customer['email'] ?? '');
?>
<section class="py-16 sm:py-20">
  <div class="max-w-[1200px] mx-auto px-4 sm:px-6">
    <div class="border border-[#1A1A1A] bg-[#030303] overflow-hidden">
      <div class="grid grid-cols-1 lg:grid-cols-12">
        <div class="lg:col-span-4 p-8 sm:p-12 border-b lg:border-b-0 lg:border-r border-[#1A1A1A] bg-[#0A0A0A]">
          <span class="font-mono text-[10px] tracking-[0.35em] text-[#666] mb-5 block">[ ACCOUNT / HUB ]</span>
          <h1 class="text-4xl sm:text-5xl font-bold tracking-tighter leading-[0.92] uppercase">
            MY<br>ACCOUNT
          </h1>
          <p class="font-mono text-[11px] text-[#888] leading-7 lowercase mt-6">
            manage your profile, orders, billing, and support from one place.
          </p>
        </div>
        <div class="lg:col-span-8 p-8 sm:p-12 lg:p-16">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-8">
            <div class="border border-[#1A1A1A] p-5 bg-[#030303]">
              <p class="font-mono text-[10px] tracking-widest text-[#666] mb-2">PROFILE</p>
              <div class="text-white font-bold"><?= htmlspecialchars($customer['name'] ?? 'GUEST USER') ?></div>
              <div class="text-[11px] text-[#888] mt-2"><?= htmlspecialchars($email) ?></div>
            </div>
            <div class="border border-[#1A1A1A] p-5 bg-[#030303]">
              <p class="font-mono text-[10px] tracking-widest text-[#666] mb-2">LAST ORDER</p>
              <div class="text-white font-bold"><?= htmlspecialchars($lastOrder['order_number'] ?? 'NONE YET') ?></div>
              <div class="text-[11px] text-[#888] mt-2"><?= htmlspecialchars($store['checkout_status'] ?? 'idle') ?></div>
            </div>
          </div>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-px bg-[#1A1A1A] border border-[#1A1A1A]">
            <a href="<!-- #!/engine/node/ page('orders') -->" class="bg-[#030303] p-6 sm:p-8 hover:bg-[#0A0A0A] transition-colors">
              <span class="font-mono text-[10px] tracking-widest text-[#666] block mb-3">01</span>
              <div class="text-xl uppercase tracking-wide text-white">Orders</div>
            </a>
            <a href="<!-- #!/engine/node/ page('cart') -->" class="bg-[#030303] p-6 sm:p-8 hover:bg-[#0A0A0A] transition-colors">
              <span class="font-mono text-[10px] tracking-widest text-[#666] block mb-3">02</span>
              <div class="text-xl uppercase tracking-wide text-white">Cart</div>
            </a>
            <a href="<!-- #!/engine/node/ page('checkout') -->" class="bg-[#030303] p-6 sm:p-8 hover:bg-[#0A0A0A] transition-colors">
              <span class="font-mono text-[10px] tracking-widest text-[#666] block mb-3">03</span>
              <div class="text-xl uppercase tracking-wide text-white">Checkout</div>
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
